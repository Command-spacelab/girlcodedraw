# Types
type user implements Timestamps { 
  id: ID!
  name: String!
  Properties: [property]
  
  # Interface required
  createdAt: Date!
  updatedAt: Date!
}

# Inputs
input userCreateInput {
  name: String!
}

input userUpdateInput {
  name: String
}